# Server Config
server:
    port: 9196

spring:
    application:
        name: kafka-service
    kafka:
        producer:
            bootstrap-servers: localhost:9092
        consumer:
            bootstrap-servers: localhost:9092
            group-id: my-cons-group-1